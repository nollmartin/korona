import { KoronaPipePipe } from './korona-pipe.pipe';

describe('KoronaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new KoronaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
