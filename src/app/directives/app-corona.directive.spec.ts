import { AppCoronaDirective } from './app-corona.directive';

describe('AppCoronaDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCoronaDirective();
    expect(directive).toBeTruthy();
  });
});
